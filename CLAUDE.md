# CLAUDE.md — Notary Financial Control System

Baca file ini terlebih dahulu sebelum melakukan perubahan apapun pada project ini.

**Untuk perubahan non-trivial**: setelah membaca file ini, baca juga
[`docs/PROJECT_RULES.md`](docs/PROJECT_RULES.md) dan [`docs/WORKFLOW.md`](docs/WORKFLOW.md) —
keduanya mendefinisikan operational rules dan pipeline kerja (termasuk 10 subagent operasional di
[`.claude/agents/`](.claude/agents/): orchestrator, planner, architect, frontend, backend, QA,
debug, security, devops, reporter) yang dipakai di atas prinsip-prinsip di file ini.

## 1. Product Context

Software LOCAL untuk membantu finance/accounting di kantor notaris.

**BUKAN**: general-purpose accounting software, ERP, pengganti software accounting
yang sudah dipakai kantor, full General Ledger, tax filing platform, payroll,
CRM, atau AI accounting assistant.

**Tujuan**: NOTARY FINANCIAL CONTROL & TRACEABILITY SYSTEM.

Core workflow:

```
COLLECT → LINK → POSITION → TRACE
```

- **COLLECT** — Informasi finansialnya ada di mana?
- **LINK** — Informasi ini terkait Client/Matter yang mana?
- **POSITION** — Sekarang posisi finansial Client/Matter ini bagaimana?
- **TRACE** — Dari mana angka/transaksi ini berasal dan apa yang terjadi setelahnya?

Jika suatu fitur tidak membantu menjawab salah satu dari 4 pertanyaan di atas,
jangan masukkan ke MVP tanpa alasan jelas.

## 2. Discovery Findings (validated pain points)

Pain scale 0–10, dari staf accounting kantor notaris:

| # | Pain area | Score | Catatan |
|---|---|---|---|
| 1 | Uang masuk terkait client/matter | 0 | |
| 2 | Bedakan pendapatan kantor vs uang titipan | 0 | |
| 3 | Saldo uang titipan | 3 | |
| 4 | Cocokkan bank transaction vs invoice/payment | 0 | |
| 5 | Payment belum jelas client/matter | 10 | **Tapi**: "Kalau memang belum jelas milik siapa kita gaakan claim, sebelum orangnya yang bilang." → sistem TIDAK boleh memaksa identifikasi. UNLINKED = valid state. |
| 6 | Satu payment untuk beberapa invoice | 5 | |
| 7 | Partial/irregular payment | 8 | Partial sesuai struktur invoice = bukan masalah. Pain muncul saat payment TIDAK sesuai expected structure. |
| 8 | Invoice outstanding | 3 | |
| 9 | Tracking penggunaan uang titipan | 3 | |
| 10 | Tracking payment pihak ketiga | 3 | |
| 11 | Mencari bukti transaksi | 0 | |
| 12 | Riwayat lengkap transaction | 3 | |
| 13 | Duplicate payment | 0 | |
| 14 | Nominal/client/transaksi benar | 0 | |
| 15 | Posisi finansial client/matter | 10 | "biasanya buka rincian biayanya." → butuh consolidated view + drill-down ke cost detail. |
| 16 | Bank reconciliation | 0 | |
| 17 | Month-end closing | 0 | |
| 18 | Transaksi tidak tertinggal | 3 | |
| 19 | Laporan untuk notaris | 3 | |
| 20 | Menelusuri kembali transaksi | 8 | "dari rincian biaya juga." |

**Highest-value features**: unlinked payment handling (non-forced), consolidated
financial position per client/matter, cost-detail drill-down, transaction trace.

## 3. Current Source of Information (unstructured, do not assume format)

Excel, WhatsApp, bank statement, Word, rincian biaya, dokumen pendukung lain.

Jangan integrasi otomatis ke sumber-sumber ini. Gunakan konsep
SOURCE_REFERENCE / ATTACHMENT / MANUAL ENTRY. WhatsApp tidak boleh jadi database —
hanya SOURCE_TYPE = WHATSAPP dengan attachment (screenshot/export) jika perlu.

## 4. Non-Goals / Out of Scope

Full double-entry accounting engine, GL replacement, complex Chart of Accounts,
payroll, tax calculation/filing, AI autonomous decision/payment allocation,
automatic WhatsApp ingestion, automatic bank scraping, OCR pipeline (belum
diperlukan), complex approval workflow engine, multi-company ERP, procurement,
inventory, CRM penuh, HR.

Jika kebutuhan ini muncul saat development: catat di FUTURE / OUT OF SCOPE,
jangan langsung implementasikan.

## 5. Core Principle

Software membantu accounting staff TANPA mengubah proses yang sudah berjalan baik
(pain 0/10 = preserve, jangan diganti). Financial visibility & traceability =
yang harus DITINGKATKAN.

## 6. Tech Stack

- **Framework**: Next.js (App Router) — frontend (React) + backend (API routes) dalam satu app. Dijalankan sebagai satu server process di satu PC/mini-server kantor.
- **Database**: PostgreSQL — dihosting LOCAL di mesin server yang sama (bukan cloud, bukan managed service). **Direvisi dari SQLite** setelah deployment scope dikonfirmasi (lihat Riwayat Perubahan v1).
- **ORM**: Prisma.
- **Deployment model**: Satu server (Next.js + PostgreSQL) di satu PC/mini-server kantor. Staf accounting lain mengakses via browser ke IP server tersebut dalam jaringan LAN yang sama — bukan cloud, bukan multi-lokasi.
- **Attachment storage**: file disimpan di filesystem server yang sama (path tersimpan di DB), diakses semua staf melalui aplikasi — bukan network file share langsung.
- **Rationale**: proyek ini adalah software LOCAL untuk satu kantor notaris, diakses beberapa staf accounting sekaligus dari komputer berbeda (LAN, dikonfirmasi user). PostgreSQL dipilih karena didesain untuk concurrent multi-client write, sementara tetap 100% on-premise sesuai prinsip "software LOCAL" — tidak ada data yang keluar dari jaringan kantor.
- **Menjalankan (dev maupun deployment kantor)**: `docker compose up -d` — `Dockerfile` + `docker-compose.yml` membungkus app + PostgreSQL jadi satu, tidak perlu install Node/Postgres di mesin manapun selain Docker. Ini juga cara yang dipakai untuk menjalankan test scenario (Step 20) karena sandbox development tidak selalu punya Postgres terinstal.
- **UI**: Tailwind CSS v4 (`src/app/globals.css`, token warna di `@theme`) + `recharts` untuk chart Dashboard. Komponen shared di `src/components/ui/`.
- **Auth**: minimal local — tabel `staff` (nama + PIN ter-hash scrypt), session cookie signed HMAC (`src/lib/session.ts`), bukan sistem role/permission. Satu pengecualian sempit sejak v40: `staff.isAdmin` (boolean, bukan role engine) khusus menggerbangi satu aksi — reset PIN staf lain (`POST /api/staff/[id]/reset-pin`) — tidak ada aksi lain di aplikasi yang mengecek field ini. Lihat Riwayat Perubahan v4/v40 dan `UI_IMPLEMENTATION_REPORT.md` §5 untuk detail.
- **Demo data**: `npm run seed:demo` — seed lewat route handler asli (bukan raw SQL), jadi audit trail & review-status ikut ter-generate sesuai business logic sungguhan. Staf demo: PIN `1234`.

## 7. Key Design Constraints (dari master prompt, wajib dipatuhi)

1. Client/Matter harus pakai stable ID, bukan nama sebagai identifier.
2. Financial transaction TIDAK BOLEH dipaksa punya Client/Matter — UNLINKED adalah valid, permanent-if-needed state.
3. Tidak ada "auto claim" / AI automatic matching terhadap client/matter/invoice.
4. Setiap financial summary/angka harus traceable ke underlying record (clickable drill-down).
5. Financial record tidak boleh destructive delete — pakai adjustment/reversal/audit trail.
6. Reuse entity yang sudah ada sebelum membuat entity baru dengan fungsi serupa.
7. Business rule (mis. partial payment normal vs review-required) harus configurable, bukan hardcoded jadi "error".
8. Semua mutation penting (link/unlink/allocate/adjust) punya audit trail: user, timestamp, previous value, new value, reason.

## 8. Riwayat Perubahan

Tabel di bawah adalah **working memory ringkas untuk AI agent** (di-baca otomatis tiap sesi).
Changelog lengkap & human-readable ada di [`CHANGELOG.md`](CHANGELOG.md) — **itu adalah source of
truth**. Setiap kali menambah baris di sini, tambahkan juga entry yang lebih detail di
`CHANGELOG.md` (tulis di sana dulu, baru ringkas satu baris ke sini).

| Versi | Tanggal | Perubahan | File | Keterangan |
|---|---|---|---|---|
| v0 | 2026-08-10 | Inisialisasi project | CLAUDE.md | Project dimulai dari kosong (greenfield). Stack dikonfirmasi: Next.js + SQLite + Prisma. |
| v1 | 2026-08-10 | Revisi stack: SQLite → PostgreSQL | CLAUDE.md | Deployment scope dikonfirmasi user: multi-user LAN (beberapa staf accounting akses bersamaan dari komputer berbeda). SQLite berisiko untuk concurrent write lintas mesin. Database diganti PostgreSQL, tetap 100% on-premise/lokal di satu server kantor. Next.js + Prisma tidak berubah. |
| v2 | 2026-08-10 | Tambah Docker Compose | Dockerfile, docker-compose.yml, CLAUDE.md | User minta cara jalanin sistem tanpa install PostgreSQL/Node lokal. Dipakai juga untuk benar-benar menjalankan 14 skenario testing di Step 20 (sandbox awal tidak punya Postgres terinstal dan tidak ada akses sudo). |
| v3 | 2026-08-10 | MVP build lengkap (Step 1-22) selesai | seluruh src/, prisma/, MVP_SCOPE.md, SYSTEM_CONSISTENCY_REPORT.md | Step 21 menemukan 5 halaman nav yang belum pernah dibangun (`/`, `/clients`, `/transactions`, `/cost-details`, `/reports`) — ditutup. Step 22 menemukan 1 gap audit trail nyata (`recomputeReviewStatus` mengubah status otomatis tanpa log) — diperbaiki, dibuktikan test baru. Final: 9/9 MUST HAVE terbangun+teruji, 14/15 consistency check PASS, 1 WARNING (Document/Source aggregation belum lengkap, sudah dikenal & dilabeli jujur di UI), 0 FAIL. |
| v4 | 2026-08-10 | Complete UI + Application Integration | seluruh src/app, src/components, prisma (Staff table), UI_IMPLEMENTATION_REPORT.md | Built Notary Financial Control application berdasarkan Phase 1–22 functional contract: Tailwind design system, minimal local auth (staff+PIN, session cookie, lock screen), app shell (sidebar+header+⌘K search), 20 halaman (6 baru: Invoices/Payments/Deposits/Disbursements/Sources/Audit Log/Settings), 10 API endpoint baru. Menemukan & memperbaiki: audit trail gap yang sama seperti v3 tapi kali ini di call site `allocate`/`reverse` (regression test ditambah), 18 GET route yang belum enforce auth, `/api/auth/staff` yang ke-cache statis saat build (bug produksi nyata, ditemukan saat QA manual), Suspense boundary hilang di `/login`. Demo seed data ditambahkan (`npm run seed:demo`). Build/lint/typecheck/test semua PASS (23/23 test). Lihat UI_IMPLEMENTATION_REPORT.md untuk detail lengkap. |
| v7 | 2026-08-11 | Lite Mode (Dashboard chart toggle) | src/lib/liteMode.ts, src/components/LiteModeToggle.tsx, src/app/(app)/layout.tsx, src/components/AppShellClient.tsx, src/app/(app)/page.tsx | Toggle di header untuk sembunyikan 2 chart Dashboard — presentation-only, disimpan sebagai cookie (`ui_lite_mode`), bukan field DB. Server Component skip fetch data chart sekalian saat lite aktif, bukan cuma disembunyikan di client. |
| v8 | 2026-08-11 | P0/P1/P2 UX & Traceability Improvement | src/components/Sidebar.tsx, src/components/ui/Typeahead.tsx, src/lib/enums.ts, src/components/{AddCostDetailModal,NewTransactionModal,LinkDrawer,TransactionTraceView,FinancialPositionView}.tsx, src/lib/{dashboard,trace,sources}.ts, src/app/(app)/{page,transactions/page,invoices/page,matters/[id]/page,clients/[id]/page}.tsx, src/app/api/attachments/[id]/route.ts (baru), FINANCIAL_CONTROL_IMPROVEMENT_REPORT.md | Sidebar collapse/expand bug diperbaiki (root cause: header overflow di state collapsed). Typeahead deterministik baru (bukan AI) untuk Cost Category & Client search. Client→Matter cascading dikonfirmasi sudah benar. Dashboard "Needs Attention" ditambahkan. Transaction Trace: `deposit` yang sebelumnya hilang dari response sekarang tampil, invoice Outstanding per alokasi ditambahkan. Attachment download route (`GET /api/attachments/[id]`) ditambahkan — sebelumnya tidak ada sama sekali di seluruh app. Matter & Client source aggregation menutup WARNING #1 di SYSTEM_CONSISTENCY_REPORT.md. 0 dependency baru, 0 schema change. Test 32/32 (naik dari 23). Detail lengkap di FINANCIAL_CONTROL_IMPROVEMENT_REPORT.md. |
| v5 | 2026-08-11 | Docker/Prisma fix, GitHub push, dokumentasi | Dockerfile, README.md, CHANGELOG.md | `node:20-alpine` tidak punya OpenSSL sehingga Prisma migration engine gagal total di container (`prisma migrate deploy` error) — diperbaiki dengan `apk add openssl` di stage `builder` dan `runner`. Diverifikasi lewat rebuild penuh dari nol (`build --no-cache` → `up -d` → `migrate deploy` → `seed:demo`) terhadap volume Postgres baru. Repo di-push ke GitHub (`rafiimegatama/accounting-notary`). README.md dan CHANGELOG.md ditambahkan; tabel ini sejak v5 jadi ringkasan, `CHANGELOG.md` adalah source of truth. |
| v12 | 2026-08-11 | Domain refinement: Cost/Deposit terminology + Lite Mode Position view | src/components/FinancialPositionView.tsx, src/lib/sources.ts, src/components/{Sidebar,AddCostDetailModal,TransactionTraceView,GlobalSearch}.tsx, src/app/(app)/{page,clients/page,cost-details/page,invoices/[id]/page,search/page,matters/[id]/page,clients/[id]/page}.tsx | VALIDATED langsung dari akuntan kantor (Tami): COST_DETAIL = charge yang ditagihkan ke client, BUKAN actual expense kantor (bisa beda dari actual disbursement pihak ketiga); DEPOSIT bukan mekanisme default, hanya dipakai kalau memang secara bisnis diperlakukan sebagai titipan; multi-invoice payment allocation valid tapi rare. UNKNOWN/DEFERRED: selisih client charge vs actual disbursement — TIDAK dihitung sebagai profit/margin di mana pun. Implementasi: label "Total Cost"/"Cost Detail" → "Rincian Biaya" di seluruh UI (JSX text only, 0 perubahan schema/kolom/route/component name) + tooltip singkat. Lite Mode diperluas ke halaman Matter/Client Position (sebelumnya cuma nyembunyiin chart Dashboard) — 4 tile (Rincian Biaya/Invoice/Sudah Dibayar/Outstanding) pakai angka yang SAMA dengan Advanced (Sudah Dibayar = allocatedToInvoices yang sudah ada), Deposit turun jadi baris sekunder yang cuma muncul kalau ada histori deposit. Advanced mode 100% tidak berubah — semua section tetap lengkap. position.ts & exceptionRules.ts 0 diff, 42/42 test tetap pass, Outstanding dicek identik Rp10.000.000 di Advanced vs Lite untuk matter live yang sama. |
| v13 | 2026-08-11 | CSV Import + List Filters + Lock Screen 5-menit inactivity timeout | src/lib/{excelImport,inactivityTimer,lockState}.ts, src/components/{ExcelImportWizard,LockScreen,AppShellClient,ui/ClientTypeaheadField}.tsx, src/app/(app)/{transactions/import,cost-details,invoices,payments}/page.tsx | CSV import (bukan xlsx — dependency itu punya 2 CVE unpatched, diganti parser CSV tanpa dependency) sebagai entry point alternatif ke Financial Transaction, hasil akhir identik input manual (dibuktikan live: link→classify→allocate→trace lengkap). Filter tanggal+Client ditambah ke Cost Details/Invoices/Payments (scoped, bukan 6-filter penuh seperti Transactions — dinilai over-engineering untuk halaman yang biasanya diakses via drill-down). Lock Screen yang sebelumnya manual-only (tidak ada inactivity timer sama sekali) sekarang auto-lock 5 menit idle (`INACTIVITY_TIMEOUT_MS`, parameterized untuk testability, bukan config production yang visible), modal "Sesi Berakhir" muncul sekali per transisi ACTIVE→LOCKED akibat timeout, z-index dinaikkan ke atas semua modal lain (temuan integrasi nyata — sebelumnya cuma menang lewat DOM-order tie-break), pesan wrong-PIN diperbaiki ke wording spec penuh (server aslinya lebih pendek). 0 perubahan session.ts/middleware.ts/PIN hashing/schema/business logic finansial. 76/76 test pass (naik dari 69), build/lint/typecheck bersih. Tidak ada tooling browser di environment ini — verifikasi visual Lock Screen lewat pembacaan class Tailwind + penelusuran urutan mount/state, bukan screenshot nyata (dicatat sebagai limitasi jujur). |
| v14 | 2026-08-11 | Login + Lock Screen enterprise UX refinement | src/app/login/page.tsx, src/components/{LockScreen,AppShellClient}.tsx, src/lib/lockPinError.ts (baru), AUTH_UX_REFINEMENT_REPORT.md | Login di-redesign jadi split-screen enterprise (hero navy panel + form panel soft-neutral), mekanisme staf+PIN 100% tidak berubah. Lock Screen: copy modal timeout diperbaiki dari "ter-logout" (salah, bukan logout) jadi "Sesi dikunci..." (akurat); background pindah dari navy ke `bg-bg` soal-neutral yang sama dengan login (navy kini eksklusif untuk hero login); subtitle "Masukkan PIN untuk melanjutkan" yang hilang dari mockup ditambahkan; wrong-PIN copy diganti ke "PIN tidak sesuai..." sesuai spec (server tetap tidak diubah, remap di client). **Temuan keamanan/aksesibilitas nyata**: z-index tinggi sudah blokir klik tapi Tab keyboard masih bisa "bocor" ke sidebar/halaman di belakang lock (z-index tidak mengatur urutan fokus) — diperbaiki pakai atribut native `inert` (0 dependency baru, typecheck bersih di React 18.3/TS 5.5) yang sekaligus menghapus subtree dari fokus, pointer, DAN accessibility tree. Ditambah `role="dialog"`/`aria-modal`/`aria-labelledby` + `role="alert"` pada error. Logic resolusi error PIN diekstrak ke `lockPinError.ts` (pure function) agar testable tanpa DOM — 5 test baru. 0 perubahan schema/session.ts/middleware.ts/business logic finansial. 81/81 test pass (naik dari 76), build/lint/typecheck bersih. Limitasi jujur: tidak ada browser tooling di environment ini, verifikasi visual/interaksi lewat pembacaan kode+HTML bukan screenshot nyata. Detail lengkap di AUTH_UX_REFINEMENT_REPORT.md. |
| v11 | 2026-08-11 | Realistic demo dataset + idempotent seed + docker rebuild | scripts/seed-demo.ts, DEMO_SCENARIOS.md | 13 client/26 matter/76 cost detail/25 invoice/80 transaksi/32 payment/12 deposit/17 disbursement/11 attachment, seluruh 20 skenario dari brief (partial payment allowed/disallowed, overpayment, multi-invoice, unallocated, unlinked/partial-link, source pending, deposit variety) — semua lewat route handler asli, bukan raw SQL, jadi audit trail & review_status dihitung exception engine yang sudah ada (tidak di-set manual). Ditemukan & diperbaiki: seed script lama sama sekali tidak idempotent (langsung P2002 di percobaan run pertama task ini, invoice number hardcoded collide) — sekarang re-run terdeteksi & fetch instead of re-create, dibuktikan 3x run berturut-turut (run ke-2/3 hasilnya identik, 0 record baru). Ditemukan juga: docker image ternyata stale 6+ jam (SESSION_SECRET fix sebelumnya cuma recreate container, bukan rebuild image) — semua fitur P0/P1/P2/refinement tidak pernah ter-deploy ke ngrok URL sampai ketahuan saat smoke-test seed ini; sudah di-rebuild. Known artifact: 1 client+matter duplikat kosong tidak bisa dihapus karena cost_detail-nya kena trigger prevent_delete (sengaja tidak di-workaround). 0 schema/API/business logic change. |
| v10 | 2026-08-11 | Fix: SESSION_SECRET tidak ter-wire ke docker-compose (production error semua halaman) | docker-compose.yml, tests/unit/session.test.ts | Root cause: `app.environment` di docker-compose.yml cuma pernah set `DATABASE_URL`/`ATTACHMENTS_DIR`, `SESSION_SECRET` tidak pernah di-passthrough — setiap halaman panggil `requireSession()` yang akhirnya throw "SESSION_SECRET is not set." (persis match digest 3884641326 yang dilaporkan user). BUKAN disebabkan oleh P0/P1/P2 atau workflow refinement (session.ts & docker-compose.yml tidak tersentuh di keduanya) — gap deployment config yang sudah ada sejak awal. Fix: tambah `SESSION_SECRET: ${SESSION_SECRET:?...}` di docker-compose.yml (required-variable syntax, gagal keras saat `up` kalau belum di-set, bukan gagal diam-diam di page pertama). 0 perubahan kode aplikasi, 0 schema change, 0 business rule change. Test 42/42 (naik dari 40). |
| v9 | 2026-08-11 | Financial Control Workflow Refinement (Phase 2, post P0/P1/P2) | src/components/{FinancialPositionView,LinkDrawer,TransactionTraceView,UnlinkedReviewTable,BulkActionToolbar,TransactionsTable}.tsx (2 baru), src/lib/{dashboard,exceptionExplain}.ts (1 baru), src/app/(app)/{page,transactions/page,payments/page,payments/[id]/page,invoices/[id]/page,search/page}.tsx, FINANCIAL_CONTROL_WORKFLOW_REFINEMENT_REPORT.md | Drill-down: TOTAL row + formula caption (Outstanding/Deposit Remaining) di setiap tabel breakdown, pakai angka backend yang sudah ada, bukan kalkulasi baru. LinkDrawer: Client/Matter jadi 2 state terpisah + tombol "Selesaikan nanti" eksplisit. Needs Attention: kategori ke-5 "Unallocated Payments" ditambah + filter `allocationStatus` di /payments. Bulk workflow baru di Transactions (select + assign client/matter + classify) — TANPA endpoint baru, cuma loop endpoint single-entity yang sudah ada. /search di-restyle ke design system (sebelumnya inline style, satu-satunya halaman yang belum konsisten). Suggested-action deterministik (bukan AI) ditambah di Review Center & Transaction Trace. **Phase 3.4 (allocation jadi blocking) DITOLAK** — bertentangan dengan rule non-blocking Step 9 yang sudah tervalidasi; user pilih pertahankan behavior lama, tidak ada kode diubah untuk item ini. 0 schema change, 0 dependency baru, 0 endpoint baru. Test 40/40 (naik dari 32). Live regression smoke test terhadap 17 halaman semua HTTP 200. Detail lengkap di FINANCIAL_CONTROL_WORKFLOW_REFINEMENT_REPORT.md. |
| v6 | 2026-08-11 | Multi-agent development framework | docs/ (11 file baru), .claude/agents/ (10 subagent), README.md, CLAUDE.md | Dipicu oleh Q&A langsung dengan staf accounting (lihat CHANGELOG.md v6 untuk isi pertanyaan) yang menghasilkan 3 kandidat perubahan (penomoran invoice urut, koreksi payment, kategori disbursement per bank) — dicatat sebagai roadmap item, bukan langsung diimplementasi. Dibangun PRD project-wide (`docs/PRD.md`), operating rules (`docs/PROJECT_RULES.md`), system overview, workflow pipeline, coding/testing standard, deployment runbook, roadmap, plus 10 subagent operasional (orchestrator/planner/architect/frontend/backend/qa/debug/security/devops/reporter) di `.claude/agents/` yang benar-benar bisa dipanggil lewat Agent tool. Semua konten digrounding ke kode nyata (contoh route, formula, test pattern), bukan boilerplate generik. |
| v15 | 2026-08-13 | Health check endpoint + automated backups | src/app/api/health/route.ts (baru), src/middleware.ts, docker-compose.yml, docs/DEPLOYMENT.md, .gitignore | `GET /api/health` (unauth, `SELECT 1` liveness probe) dipakai oleh healthcheck baru di service `app` (sebelumnya cuma `db` yang punya). 2 service Compose baru: `backup` (image `prodrigestivill/postgres-backup-local`, scheduled `pg_dump` + retensi) dan `attachments-backup` (tar+rotate untuk volume attachments) — ke-2nya bind-mount ke `./backups/`, disk yang sama dengan data live sehingga tetap butuh staf copy manual ke media eksternal (didokumentasikan jujur, bukan auto-off-site). Ditemukan & diperbaiki insidental: `app` container serving stale image (kelas bug sama dengan v11) — di-rebuild. 114/114 test pass, security review PASS (2 catatan non-blocking, tidak diperbaiki di sini). 0 schema change. Detail di CHANGELOG.md v15. |
| v16 | 2026-08-13 | Source Reference Typeahead di New Transaction | src/app/api/transactions/source-references/route.ts (baru), src/components/NewTransactionModal.tsx | `GET /api/transactions/source-references?search=` (auth required, min 2 char, max 8 hasil, substring case-insensitive — pola sama persis dengan `/api/clients?search=`) mengisi field "Source Reference" yang sebelumnya `<input>` polos, sekarang reuse `Typeahead` (v8) dengan debounce 250ms. Free text tetap valid, suggestion opsional — tidak ada auto-claim. Scoped hanya ke `FinancialTransaction.sourceReference` (bukan `CostDetail.sourceReference`) karena modal ini cuma create `FinancialTransaction`. 0 schema change, 0 dependency baru, tidak ada test baru (thin wiring di atas komponen & pattern yang sudah teruji), security review dilewati (dinilai tidak perlu — mirror pattern yang sudah direview). 114/114 test tetap pass. Detail di CHANGELOG.md v16. |
| v17 | 2026-08-13 | Document Upload di Client/Matter Position + upload entry-point sizing | src/components/UploadDocumentModal.tsx (baru), src/app/(app)/clients/[id]/page.tsx, src/app/(app)/matters/[id]/page.tsx, src/app/(app)/transactions/page.tsx | Menutup gap nyata: `POST /api/attachments` sudah ada sejak sebelumnya tapi tidak pernah punya UI (cuma link download). `UploadDocumentModal` (pola sama dengan `AddCostDetailModal`) dipasang di Client & Matter Position saja (sesuai scope diminta), 0 perubahan backend/schema. Tombol trigger dibuat `size="md"` + ikon upload (lebih besar dari sibling `sm`) dan link "Import Excel" di /transactions direstyle jadi solid button — keduanya respons langsung ke feedback UX user bahwa entry point upload sebelumnya terlalu kecil/gampang terlewat; wizard CSV import sendiri tidak disentuh. Live-verified upload+download nyata (client Andi Pratama, matter AJB Tanah Kavling). 115/115 test pass, 0 test baru, security review dilewati (mirror endpoint yang sudah diaudit). Detail di CHANGELOG.md v17. |
| v18 | 2026-08-13 | 4 Roadmap item paralel (invoice numbering, payment correction, allocation balance indicator, per-bank disbursement) + fix security HIGH (race condition void/correct) | src/lib/financialTransactionActions.ts (baru), src/app/api/payments/[id]/correct/ (baru), src/app/api/bank-accounts/ (baru), prisma/schema.prisma + migrations/20260812155645_add_bank_account (baru), ddl_notary_financial_control.sql, src/app/api/invoices/route.ts, src/lib/invoiceNumbering.ts, src/app/api/transactions/{route.ts,[id]/void/route.ts,[id]/classify/route.ts}, src/components/{CreateInvoiceModal,TransactionActions,TransactionTraceView,FinancialPositionView}.tsx, src/lib/{trace,position}.ts, src/app/(app)/disbursements/page.tsx | `docs/ROADMAP.md` #1–3 (staf accounting, 2026-08-11) + `PAINKILLER_COVERAGE_AUDIT.md` §7.4, dikerjakan paralel via subagent (architect→backend/frontend→security→QA). (1) Suggested next invoice number per-tahun (`INV-2026-001`, dikonfirmasi ke kantor) + `sequenceWarning` non-blocking. (2) `POST /api/payments/[id]/correct` = void+create atomic dalam satu `prisma.$transaction`, helper diekstrak ke `financialTransactionActions.ts` dan dipakai ulang oleh route void/create asli (behavior-preserving); link lama↔baru cuma di audit trail (0 schema change), tampil di Trace sebagai "Dikoreksi oleh"/"Mengoreksi". (3) `AllocateInline` tampilkan sisa tagihan invoice live, informational-only, tidak mengubah rule non-blocking. (4) `BankAccount` table baru (mirror pola Client, BUKAN pola CostCategory — office confirmed set tetap) + `Disbursement.bankAccountId` nullable FK `ON DELETE RESTRICT` (1 migration additive, diverifikasi 2x). **Security HIGH** (ditemukan security-agent sebelum ship): `assertVoidable` cuma check di luar transaction, tidak ada re-check di dalam → 2 void/correct konkuren bisa dua-duanya lolos → diperbaiki jadi conditional `updateMany(status:ACTIVE→VOIDED)` di dalam `voidFinancialTransactionTx`, Postgres row lock jadi guard sungguhan, dibuktikan test interleaved-transaction asli (gagal tanpa fix, lulus dengan fix); 2 LOW dibundel (reason kosong/whitespace, newAmount non-numeric). **QA-caught**: bank account tersimpan tapi tidak pernah tampil di UI (read model tidak `include`) — diperbaiki di `trace.ts`/`position.ts` + 3 UI surface, regression test dibuktikan gagal dulu baru lulus. 116/116 test (naik dari 115 tercatat di v17), build/lint/typecheck bersih, live-verified via Docker rebuild+redeploy+health-check tiap ada perubahan backend. Detail lengkap di CHANGELOG.md v18. |
| v19 | 2026-08-13 | Contextual Field Help | src/components/ui/FieldHelp.tsx (baru), src/lib/fieldHelp.ts (baru), src/components/{AddCostDetailModal,NewTransactionModal,TransactionActions,CreateInvoiceModal,FinancialPositionView,TransactionsTable}.tsx, src/app/(app)/{payments/[id]/page.tsx,review/page.tsx} | Komponen "(?)" baru (native `<button>`, buka lewat hover/focus/tap, bukan CSS `:hover`-only, dismiss via Escape/click-outside, popover `position: absolute` jadi tidak geser layout) + `FIELD_HELP` sebagai satu sumber copy tooltip. Dipasang di 6 field P0 (Cost Detail Amount, Financial Type, Source Type, Source Reference, Allocation Amount, Allow Partial Payment) + 6 field P1 (Deposit classify label, Deposit Used, Invoice Total, Payment Amount, Link Status header, Review Status heading), placement disesuaikan per tipe kontrol (inside-field/label-side/adjacent-flex/after-label). `SummaryStat` dikonsolidasi: `title` attribute lama yang mirip-mirip dihapus, diganti mekanisme sama. 0 schema/API/business-logic change, 0 test baru (116/116 tetap, sama dengan v18 — thin UI wiring), tidak ada review browser nyata (limitasi sudah dicatat sejak v13/v14). Detail lengkap di CHANGELOG.md v19. |
| v20 | 2026-08-13 | Remaining Balance & Allocation Preview di AllocateInline | src/lib/allocationPreview.ts (baru), tests/unit/allocationPreview.test.ts (baru), src/components/TransactionActions.tsx, src/app/(app)/payments/[id]/page.tsx, src/components/TransactionTraceView.tsx | Live, local preview (tanpa network request) di widget alokasi Payment→Invoice yang sudah ada: breakdown invoice terpilih (Total/Sudah dialokasikan/Sisa, gantikan single-line v18) + baris "Setelah alokasi" (Payment tersisa/Sisa invoice) + 2 warning independen (`⚠`, `text-warning`, non-blocking, bisa muncul bareng) untuk exceed-payment dan exceed-invoice. `computeAllocationPreview()` pakai plain-number arithmetic yang sengaja sama gaya dengan `invoiceOutstanding()` yang sudah ada di file yang sama. Prop `unallocated` di `AllocateInline` diubah dari string terformat jadi `number` mentah, 2 call site diupdate. 0 schema/API/business-logic change (Simpan tetap tidak diblok, exception engine backend tetap satu-satunya otoritas review_status). Test 122/122 (naik dari 116, 6 baru). Detail lengkap di CHANGELOG.md v20. |
| v21 | 2026-08-13 | Calculation Transparency ("Bagaimana dihitung?") di Client/Matter Financial Position | src/components/ui/CalculationExplain.tsx (baru), src/lib/summaryDerived.ts (baru), tests/unit/summaryDerived.test.ts (baru), src/components/FinancialPositionView.tsx | Disclosure "Bagaimana dihitung?" baru, sengaja terpisah visual dari ikon "(?)" Field Help v19 (pertanyaan beda: asal-usul angka vs arti field), mirror pola aksesibilitas Field Help (native `<button>`, aria-*, Escape/outside-dismiss) tapi TIDAK close-on-blur karena popover berisi link drill-down yang harus tetap tab-reachable. Dipasang di 8 KPI tile Advanced mode (Rincian Biaya, Total Invoice, Total Payment, Outstanding, Unallocated, Deposit Received/Used/Remaining) — tiap popover tampilkan formula (`FormulaRows`) dan/atau daftar record kontributor asli yang sudah ter-load di halaman (`ListBreakdown`, 0 fetch baru), tiap baris link ke rute/anchor yang sudah ada. `computeAllocatedAmount()` baru jadi satu tempat `Total − Remaining` dihitung, gantikan 2 ekspresi inline terpisah yang sebelumnya bisa drift. Box "formula caption" permanent lama dihapus (`FormulaCaption` dibuang), isinya diserap ke disclosure Outstanding/Deposit Remaining. Lite Mode tidak disentuh. Nuance lama yang sudah ada (Client-scope payments/deposits/disbursements cuma bucket "belum ter-assign ke matter") dibiarkan apa adanya, ditampilkan jujur di popover, tidak diperbaiki (di luar scope). 0 schema/API/business-logic change. Test 126/126 (naik dari 122, 4 baru). Detail lengkap di CHANGELOG.md v21. |
| v22 | 2026-08-13 | Success → Next Action toast feedback + Keyboard-first UX (focus trap) | src/lib/toastNextHints.ts (baru), tests/unit/toastNextHints.test.ts (baru), src/components/ui/useModalFocusTrap.ts (baru), src/components/ui/Toast.tsx, src/app/globals.css, src/lib/trace.ts, 10 komponen lain | Toast sukses opsional dapat baris "next action" saran (bukan perintah, Indonesia, kondisional per state — tidak pernah menyarankan hal yang belum bisa diketahui, mis. allocate DEPOSIT atau link ke Matter saat Client belum punya matter) via 4 fungsi murni di `toastNextHints.ts`, dipasang di 6 flow create/classify/allocate; sengaja TIDAK dipasang di `LinkDrawer` (dipertahankan pola inline "Linked to X [Undo]" existing, dikonfirmasi user) maupun di `AddCostDetailModal`/`ReverseAllocationButton` (CTA akan self-referential). Bug ditemukan+diperbaiki: `CreateClientForm` reload halaman sebelum toast sempat render — diganti `router.refresh()`. Keyboard-first: bug CSS app-wide `.input:focus{outline:none}` yang menutupi `:focus-visible` ring diperbaiki; hook baru `useModalFocusTrap` (0 dependency baru) dipasang ke 9 modal/drawer yang sebelumnya tanpa trap/Escape/focus-return sama sekali, Escape mengalah ke popover Field Help/CalculationExplain/Typeahead yang sedang terbuka. Bug pre-existing ditemukan tidak sengaja (bukan bagian dari 2 tujuan di atas): Transaction Trace mengirim `FinancialTransaction.id` bukan `Payment.id` asli ke `AllocateInline` sehingga Allocate Payment dari halaman Trace selalu 404 sejak v20 — diperbaiki dengan expose `paymentId` asli di `buildTransactionTrace()` (0 schema change), dibuktikan regression test gagal-lalu-lulus. 0 schema/API/business-logic change. Test 138/138 (naik dari 126, 1 baru). Tidak ada review browser nyata (limitasi sudah dicatat sejak v13/v14/v19/v21). Detail lengkap di CHANGELOG.md v22. |
| v23 | 2026-08-13 | Dashboard chart complementarity fix: Unlinked Backlog by Age + Financial Activity click-through | src/lib/dashboard.ts, src/components/charts/UnlinkedAgingChart.tsx (baru, ganti UnlinkedTrendChart.tsx yang dihapus), src/components/charts/FinancialTrendChart.tsx, src/app/(app)/page.tsx, tests/scenarios/masterPromptScenarios.test.ts | Dipicu UX audit: "Unlinked Transactions Trend" bukan over-engineering secara konsep (jawab pain #5, 10/10) tapi eksekusinya bermasalah — `range` param diam-diam kesambung ke chart Financial Activity yang picker-nya beda baris, dan chart-nya hybrid snapshot (filter status link saat ini) vs trend (bucket by tanggal transaksi asli) yang bisa menyesatkan. `getUnlinkedTrend()` diganti `getUnlinkedBacklogAging()` — snapshot un-windowed, bucket by usia (0-7/8-30/31-60/>60 hari), count sebagai metrik utama, satu warna flat amber (`#D97706`, BUKAN ramp eskalasi merah seperti `AgingBarChart`) karena UNLINKED boleh permanent by design (CLAUDE.md §7 #2) — usia tua bukan berarti mendesak. Tiap bucket klik ke `/transactions?linked=unlinked&dateFrom=X&dateTo=Y` (param existing, 0 route baru). Financial Activity Over Time juga ditambah click-through per tanggal (sebelumnya 2 dari 4 chart Dashboard tanpa drill-down sama sekali, melanggar §7 constraint 4). 0 schema/API change. Test 139/139 (naik dari 138, 1 baru — invariant cross-widget: total 4 bucket = `getDashboardSummaryCards().unlinked`). Catatan non-blocking dari QA (belum diperbaiki, bukan bug baru): bucket-day-math pakai local midnight sedangkan `transactionDate` UTC `@db.Date`, sama seperti pola existing di `getOutstandingAging()`. Detail lengkap di CHANGELOG.md v23. |
| v24 | 2026-08-13 | Recent Client/Matter (Feature C) + Keyboard-first UX audit closeout (Feature B) | src/lib/recentSelections.ts (baru), tests/unit/recentSelections.test.ts (baru), src/components/ui/Typeahead.tsx, src/components/{NewTransactionModal,LinkDrawer,TransactionActions}.tsx | Lanjutan spec UX yang sama dengan v19–v23: Feature A (toast next-hint) & Feature B (focus trap) ternyata sudah lengkap lewat v22 (`toastNextHints.ts`, `useModalFocusTrap.ts`) — diverifikasi ulang di pass ini, tidak disentuh. Feature C (Recent Client/Matter) genuinely belum ada (grep 0 hit untuk `recentClient`/`recentMatter`/localStorage-based selection sebelum ini) — `recentSelections.ts` baru (localStorage-only, tidak pernah ke server, key prefix `notary:recentClients`/`notary:recentMatters`, cap 5/list, MRU, dedupe-on-reselect, try/catch di semua read/write) dipasang lewat `Typeahead`'s `recentOptions` prop baru (opsional, additive) di 2 titik pilih Client/Matter nyata (`NewTransactionModal`, `LinkDrawer`) — sekaligus memperbaiki bug nyata di kedua call site: `onSelect` handler resolve opsi lewat `clientResults.find()` yang tidak pernah berisi opsi Recent, jadi klik Recent sebelumnya silently no-op. Audit closeout Feature B (verifikasi saja, bukan rewrite): tab order PASS (0 `tabIndex` override di `src/`), table keyboard reachability PASS (0 `<tr onClick>`), Enter behavior PASS setelah 1 bug baru ditemukan+diperbaiki — `AllocateInline` (widget alokasi Payment→Invoice) bukan `<form>` sehingga Enter tidak submit apa-apa, dibungkus `<form onSubmit>` (3 modal destructive Void/Reverse/Correct sengaja tetap bukan form, tidak diubah — exception itu tidak berlaku di sini karena alokasi reversible). Known limitation (didokumentasikan di komentar module): buffer mentah recent matter digabung lintas client (cap 30 total, bukan per-client key) — list yang DITAMPILKAN tetap benar cap-5-per-client-MRU. `BulkLinkPanel` (3rd picker, `BulkActionToolbar.tsx`) sengaja tidak disentuh (di luar 2 call site yang diminta), dicatat sebagai kandidat di `docs/ROADMAP.md`. 0 schema/API/endpoint baru. Test 152/152 (naik dari 139, 13 baru). Tidak ada review browser nyata (limitasi sejak v13/v14). Security review dilewati (localStorage-only, tidak pernah ke server, tidak menyentuh auth/session). Detail lengkap di CHANGELOG.md v24. |
| v25 | 2026-08-13 | Printable Client/Matter Financial Report ("Laporan untuk Notaris") | src/components/PrintReportButton.tsx (baru), src/components/{FinancialPositionView,AppShellClient,Sidebar}.tsx, src/app/{clients/[id],matters/[id]}/page.tsx, src/app/globals.css | Menutup pain point #19 (`CLAUDE.md §2`) dan FR-24 di `docs/PRD.md` (sebelumnya "out of scope — no PDF report engine"). Tombol "Cetak Laporan" panggil `window.print()` native browser (0 dependency baru, "Save as PDF" di dialog print browser = jalur PDF-nya) dipasang di header `FinancialPositionView` — otomatis muncul di Client & Matter Position sekaligus (1 komponen shared). Print stylesheet (`print:hidden`/`hidden print:block` + `@page`/`break-inside:avoid` di globals.css) menyembunyikan chrome/help-trigger/Timeline/Sources, menampilkan title+KPI+semua tabel breakdown termasuk TOTAL row. Fix layout nyata: shell (`h-screen`/`overflow-hidden`/`overflow-y-auto`) di-override `print:*` di 3 level supaya print tidak terpotong ke viewport yang lagi ke-scroll. **Bug ketemu saat changelog review, langsung diperbaiki**: header print sempat pakai `BRANDING_TEXT_DEFAULTS.branding_hero_eyebrow` (default constant) bukan setting live — diperbaiki dengan `getBrandingSettings()` ditambah ke `Promise.all` yang sudah ada di kedua page, dialirkan sebagai prop `brandingEyebrow` baru. 0 schema/API change, 0 test baru (154/154 tetap, pure CSS+data-plumbing). Tidak ada verifikasi print preview nyata (tidak ada browser tooling) — ditandai eksplisit sebagai item paling butuh dicek manusia. Detail lengkap di CHANGELOG.md v25. |
| v26 | 2026-08-13 | Painkiller #10 (ringkasan disbursement per bank account) + #18 (headline "X dari Y baris ter-import") | src/lib/listAggregates.ts, src/app/(app)/disbursements/page.tsx, src/components/ExcelImportWizard.tsx, tests/scenarios/masterPromptScenarios.test.ts | Menutup 2 pain point `CLAUDE.md §2` yang masih "score 3" (belum sepenuhnya tertutup): #10 "Tracking payment pihak ketiga" dan #18 "Transaksi tidak tertinggal". `BankAccount`/`Disbursement.bankAccountId` sudah ada sejak v18 tapi belum ada view "total keluar per rekening lintas matter" — ditutup lewat `getDisbursementSummaryByBankAccount()` baru (uncapped, beda dari list page yang `take: 150`), Card baru di `/disbursements` + filter `?bankAccountId=<id\|none>` (sentinel `"none"` untuk yang belum tercatat rekeningnya, ditampilkan jujur bukan disembunyikan). Import CSV: headline baru "X dari Y baris ter-import, Z di-skip" di layar hasil — murni gabungan 2 angka yang sudah ada di state komponen, eksplisit BUKAN reconciliation engine baru. 0 schema/migration change. QA buktikan langsung ke data nyata bahwa uncapped-query itu load-bearing (200 disbursement dimasukkan, list capped `take:150` akan under-report ~25% kalau dipakai buat summary). Test 154/154 (2 baru, dihitung ulang manual di pass ini: 154 `it()` di 15 file). **Temuan insidental QA, belum diperbaiki**: `docker compose build app` gagal (`invalid file request backups/db/daily/...`, direproduksi ulang independen) karena file backup dari service v15 (`backup`/`attachments-backup`) berupa symlink dan `.dockerignore` tidak exclude `backups/` sama sekali — dicatat sebagai item baru di `docs/ROADMAP.md` Mid-Term untuk devops-agent, tidak diperbaiki di pass ini. Detail lengkap di CHANGELOG.md v26. |
| v27 | 2026-08-14 | LAN/WiFi deployment hardening: Postgres port exposure + restart policy | docker-compose.yml, docs/DEPLOYMENT.md | Dipicu review arsitektur deployment ("apakah cukup untuk kantor LAN/WiFi-only"). 2 gap ditemukan+diperbaiki di `docker-compose.yml` (0 app code): `db` port mapping `"5432:5432"` → `"127.0.0.1:5432:5432"` (Postgres sebelumnya reachable dari perangkat manapun di LAN/WiFi, bukan cuma app container yang sudah punya akses lewat internal Compose network `db:5432`); `app`+`db` sekarang punya `restart: unless-stopped` (sebelumnya cuma `backup`/`attachments-backup` yang punya, jadi power blip/reboot butuh restart manual). `docker compose config` divalidasi bersih. **Belum dikerjakan, sengaja dipisah sebagai keputusan tersendiri**: TLS/HTTPS (temuan bernilai tertinggi untuk WiFi — saat ini plaintext HTTP, cookie session & PIN login bisa disadap dalam radius WiFi), rate-limit/lockout login (PIN attempt saat ini tidak dibatasi, staff list sengaja publik untuk login picker), security headers Next.js (belum ada `next.config.js`). 0 schema/API/business-logic change. |
| v28 | 2026-08-14 | Deployment review lanjutan: security headers + backup cadence + panduan server wired | next.config.js (baru), docs/DEPLOYMENT.md, docs/SYSTEM_OVERVIEW.md | Menutup sisa temuan #5–#7 dari review v27. `next.config.js` baru (sebelumnya tidak ada sama sekali) — CSP + X-Frame-Options + X-Content-Type-Options + Referrer-Policy + Permissions-Policy, digrep dulu memastikan 0 external script/style/image source di `src/` jadi CSP bisa `default-src 'self'` (script-src/style-src tetap `'unsafe-inline'` — App Router butuh inline `<script>` untuk RSC streaming, nonce-based policy lebih ketat butuh perubahan middleware.ts, sengaja tidak dikerjakan karena tidak ada browser tooling untuk verifikasi live). Diverifikasi lewat `docker build --target=builder` sungguhan (bukan cuma baca kode) — build sukses, 40 route ter-compile. `docs/DEPLOYMENT.md` §5: cadence off-machine backup yang tadinya cuma "periodically" jadi konkret (mingguan, verifikasi file bukan cuma percaya dialog copy, harus ada penanggung jawab bernama) — 0 kode baru, ini murni memperjelas instruksi manual yang sudah sengaja tidak diotomasi (`CLAUDE.md §4`). `docs/DEPLOYMENT.md` §9 + `docs/SYSTEM_OVERVIEW.md` §6: panduan operasional (bukan enforceable di kode) — server sendiri sebaiknya kabel Ethernet meski kantor WiFi-only untuk staf, supaya WiFi jelek di server tidak menjatuhkan semua sesi staf sekaligus. 0 schema/API/business-logic change. |
| v29 | 2026-08-14 | TLS untuk akses LAN (Caddy) + cookie `Secure` flag di balik opt-in cutover | docker-compose.yml, Caddyfile (baru), src/lib/session.ts, src/app/api/auth/{login,logout}/route.ts, .env.example, docs/DEPLOYMENT.md | Menutup temuan bernilai tertinggi yang sengaja ditunda di v27 (TLS/HTTPS — PIN & cookie session sebelumnya plaintext, bisa disadap dalam radius WiFi, cookie curian bisa di-replay hingga 12 jam). `caddy` service baru (`caddy:2-alpine`, `tls internal` — CA lokal sendiri, bukan public cert, karena akses via LAN IP bukan domain publik) di port `443` baru, bersifat ADDITIF — mapping `app` `3000:3000` tidak disentuh karena tunnel ngrok sementara (`docs/DEPLOYMENT.md` §7) mengarah langsung ke situ dan harus tetap jalan. `cookieSecure()` helper baru di session.ts baca env `COOKIE_SECURE` (default `"false"`) — SENGAJA tidak langsung di-hardcode `true` karena sandbox ini tidak ada Docker untuk verifikasi live jalur Caddy-nya; `docs/DEPLOYMENT.md` §10 baru berisi runbook: bawa naik `caddy`, export root CA-nya ke tiap mesin staf (manual, one-time, sesuai non-goal `CLAUDE.md §4` soal PKI automation), verifikasi `https://<server-ip>` bersih, BARU set `COOKIE_SECURE=true`. tsc/build/lint bersih (1 error pre-existing tak terkait di callApi.ts), 106/106 unit test pass. **Belum diverifikasi live** (tidak ada Docker di sandbox ini, sama seperti limitasi v11/v15/v26) — `caddy` service, cert-trust, dan cutover `COOKIE_SECURE=true` masih butuh `docker compose up -d` + cek browser sungguhan sebelum dianggap selesai. 0 schema/business-logic change. |
| v30 | 2026-08-14 | Cutover TLS v29 dijalankan live + bug on-demand issuance ditemukan+diperbaiki | Caddyfile, .gitignore | Session ini punya akses Docker sungguhan ke deployment yang benar-benar berjalan, jadi cutover v29 (yang sebelumnya cuma tervalidasi sintaks) dieksekusi nyata: `docker compose up -d` (db+app ter-recreate karena config berubah, caddy baru dibuat) — dipicu spesifik karena tunnel ngrok yang sedang dipakai staf akuntansi untuk testing mengarah ke `app` port `3000`, harus dibuktikan selamat, bukan cuma diasumsikan. **Dikonfirmasi live**: downtime `app` ~20 detik, port 3000 (target ngrok) kembali `200` di `/api/health`+`/login` segera setelah recreate, tidak berubah sama sekali. **Bug nyata ditemukan** (tidak ketangkap `caddy validate` v29 karena itu cuma cek syntax, bukan runtime): blok `:443` tanpa hostname tetap butuh `tls internal { on_demand }` eksplisit — tanpa itu, Caddy menolak generate cert untuk SNI/IP yang tidak dikenal di awal, setiap handshake TLS sungguhan gagal (`TLS alert: internal_error`), ditemukan lewat percobaan handshake nyata (container curl terpisah di Docker network yang sama, bukan cuma baca kode). Diperbaiki + diverifikasi ulang dengan chain-of-trust proper (bukan `-k` skip-verify) — `https://caddy/login` dan `/api/health` sama-sama `200` tervalidasi penuh terhadap CA yang di-export (`office-root-ca.crt`, sekarang di `.gitignore` — public cert, bukan secret, tapi spesifik-instance). `COOKIE_SECURE` sengaja TIDAK di-flip ke `true` di pass ini — sisa langkah manual (distribusi CA ke tiap mesin staf, verifikasi dari browser staf sungguhan) butuh akses fisik yang tidak tersedia dari sini; flip prematur akan mengunci staf yang masih pakai `http://<ip>:3000` lama. 0 schema/business-logic change. |
| v31 | 2026-08-14 | 7-item infra/DevOps/DBA audit remediation (login lockout, backup restore diverifikasi+bug ditemukan, resource limits, log rotation, alerting decision, onDelete drift diperbaiki, image reproducibility) | src/lib/loginRateLimit.ts (baru), tests/unit/loginRateLimit.test.ts (baru), src/app/api/auth/login/route.ts, scripts/{restore-drill.sh,check-health.sh} (baru), docker-compose.yml, prisma/schema.prisma, prisma/migrations/20260814180000_fix_ondelete_restrict (baru), Dockerfile, docs/DEPLOYMENT.md | Menutup seluruh item High+Medium dari audit infra/DevOps/DBA read-only terhadap state v27-v30. Docker akses live tersedia di session ini (stack sudah jalan dari cutover v30, tunnel ngrok aktif dipakai staf akuntansi) — tiap item diverifikasi live terhadap stack nyata, disusun urutannya khusus supaya ngrok tidak putus (service non-app duluan, `app` cuma di-recreate sekali menggabungkan semua perubahan level-app). **High**: login lockout per-staffId (5x gagal = kunci 5 menit, in-memory, keyed staffId bukan IP) — smoke-test live: percobaan 1-5 `401`, ke-6 `429`. Restore drill (`scripts/restore-drill.sh`, non-destructive ke scratch DB) — run pertama GAGAL, menemukan bug nyata: image `backup` (`prodrigestivill/postgres-backup-local:latest`) bundle `pg_dump` 18 vs server `postgres:16-alpine`, `pg_dump` 18 nulis `SET transaction_timeout` (parameter Postgres 17+) yang bikin restore gagal — backup harian selama ini sukses tapi diam-diam TIDAK bisa di-restore. Diperbaiki: pin image ke tag `16-alpine`, diverifikasi ulang live (row count cocok: 3 staff/16 client/26 matter/86 financial_transaction/367 audit_log). **Medium**: resource limits + log rotation (`x-logging` YAML anchor + `deploy.resources.limits` di 5 service, titik awal bukan hasil ukur hardware asli) diterapkan live tanpa sentuh container `app`. Alerting: keputusan eksplisit TIDAK integrasi cloud/email (`CLAUDE.md §4`), `scripts/check-health.sh` baru sebagai jalan tengah zero-dependency. `onDelete` drift: DDL selalu dokumentasikan RESTRICT untuk 9 FK opsional, `schema.prisma` tidak pernah deklarasi eksplisit jadi default Prisma (SET NULL) yang sungguhan ter-apply sejak migration pertama — diperbaiki DB-nya supaya cocok niat yang didokumentasikan (bukan sebaliknya), migration ditulis tangan (tidak ada DB live saat edit schema), diterapkan via `prisma migrate deploy`, dikonfirmasi `pg_constraint.confdeltype='r'` untuk semua 9. 5 index parsial `FinancialAttachment` (`idx_attach_*`) didokumentasikan di schema.prisma sebagai sengaja TIDAK dideklarasikan `@@index` (Prisma schema DSL tidak dukung partial/filtered index). Dockerfile: `npm install` tanpa lockfile di context → `npm ci` + stage `prod-deps` baru (prod-only node_modules + Prisma client generated di-copy terpisah dari `builder`) — devDependencies dikonfirmasi hilang dari image final. Full test suite 160/160 (naik dari 154, +6 baru) dijalankan ulang dari `prisma migrate deploy` bersih — 0 regresi. Downtime ngrok: `db` recreate ~blip singkat (Prisma auto-reconnect), `app` recreate ~20 detik sekali gabungan semua perubahan, dikonfirmasi live `curl` ke localhost:3000, Caddy https, DAN URL publik ngrok — ketiganya `200` segera setelah. `COOKIE_SECURE` tidak disentuh (masih `false`, sesuai catatan v30). 0 API/route-shape change, 1 schema change (onDelete only). |
| v32 | 2026-08-14 | Halaman statis "Panduan Alur Kerja" | src/app/(app)/guide/page.tsx (baru), src/components/Sidebar.tsx | Dipicu langsung oleh staf akuntansi (Tami) bilang "pusing" lihat Needs Attention muncul tanpa paham kenapa — dijelaskan dulu secara langsung (bukan kode), lalu dibuatkan versi permanennya di sini setelah dikonfirmasi cukup halaman statis, TIDAK perlu full tour interaktif (opsi itu diajukan eksplisit lalu ditolak: UI app ini sudah ganti 30+ versi CHANGELOG, coachmark per-tombol lintas halaman akan jadi hal paling rapuh/mahal maintenance-nya di codebase, untuk kebutuhan yang baru muncul sekali, belum tervalidasi berulang). 3 bagian: (1) timeline 7 langkah Client/Matter sampai invoice lunas, tiap langkah link ke halaman asli; (2) accordion (native `<details>/<summary>`, 0 dependency, aksesibel keyboard/screen-reader gratis) untuk 6 kategori Needs Attention — tone warna dan href disalin persis dari `page.tsx`'s `NeedsAttention` supaya tidak mungkin kontradiksi apa yang dia lihat sungguhan; (3) 6 skenario kasus nyata pakai angka Rupiah konkret (termasuk kasus overpayment yang persis dialami Tami, di-expand default). Ditutup callout 4 prinsip penting. Sidebar dapat entry baru langsung di bawah Dashboard. Live-verified end-to-end: login pakai staf demo sungguhan, `GET /guide` 200, semua bagian konten dikonfirmasi ada di HTML asli. Deploy pakai pola build-dulu-baru-swap yang sama seperti v31 — dikonfirmasi `200` di localhost, Caddy https, DAN URL publik ngrok sepanjang proses. 0 schema/API/business-logic change, 0 dependency baru. |
| v33 | 2026-08-14 | Section "Cek Transaksi Tertentu" di guide, nutup gap sisa dari v32 | src/app/(app)/guide/page.tsx | v32 direview ulang terhadap pertanyaan asli Tami, ketemu baru setengah tertutup: skenario di v32 cuma ajarin pola umum, bukan cara investigasi SATU record nyata spesifik (pertanyaan aslinya: "menu apa yg tiba2 menyebabkan ini" untuk transaksi 12 Agustus yang dia klik beneran) — app sudah punya tool persis buat itu (Transaction Trace/Timeline, sudah lama ada, bukan baru), gap-nya cuma guide tidak pernah nyebut itu. Section 4 baru "Nemu Kasus Spesifik? Cek Transaction Trace" (Prinsip Penting geser jadi section 5) — walkthrough 5 langkah, label disalin PERSIS dari heading asli `TransactionTraceView.tsx` (Timeline/Relationships/Audit) supaya instruksi tidak pernah bilang sesuatu yang beda dari yang muncul di layar (prinsip senior UX: copy instruksional harus match label asli, bukan parafrase). Diframe sebagai skill reusable (bisa dipakai investigasi kategori Needs Attention manapun), bukan tempelan khusus overpayment doang. 0 fitur baru — Trace/Timeline/saran "Aksi:" (`exceptionExplain.ts`) semua sudah ada, ini cuma ngajarin cara pakainya. Live-verified sama seperti v32 — login staf demo sungguhan, `GET /guide` 200, konten section baru dikonfirmasi ada di HTML. Deploy build-dulu-swap sekali, ngrok `200` sepanjang proses. 0 schema/API/business-logic change, 0 dependency baru. |
| v34 | 2026-08-14 | 6-item guide "painkiller" pass: contextual link, skenario deposit/disbursement, CSV/bulk mention, TL;DR, live situasi strip | src/app/(app)/{page.tsx,review/page.tsx,guide/page.tsx} | Self-critique atas guide v32/v33 sebagai solution-analyst+UI/UX+accountant-ops, ketemu masih "dokumen referensi" bukan "painkiller" (cuma ketemu kalau inget buka halamannya) + konten belum lengkap (cuma sisi payment masuk, belum deposit/disbursement/CSV import/bulk action). Semua 6 temuan dikerjakan sekaligus atas instruksi user. Fix ter-leverage: ikon "?" kontekstual di tiap baris Needs Attention Dashboard → `/guide#<kategori>` (reuse pola FieldHelp/CalculationExplain, bukan infrastruktur baru) — butuh restructure row dari 1 `<a>` penuh jadi 2 link sibling (nested `<a>` invalid HTML). Link serupa di Review Center → `/guide#trace`. 2 skenario baru (Deposit, Disbursement) — 6 skenario lama semuanya soal uang masuk doang. CSV Import + bulk-select disebut di step 4 timeline dan action Unlinked — fitur nyata yang sudah ada tapi sebelumnya tidak pernah disebut di guide. TL;DR 4 bullet di atas quick-jump. Strip "Situasi kamu sekarang" — pakai `getDashboardSummaryCards()` yang sama persis dengan Dashboard (0 query baru), jembatani contoh generik Rp10jt ke angka real dia; `GuidePage` jadi async Server Component buat ini. Mobile responsiveness dicek lewat baca class layout (semua row pakai flex-wrap) — TIDAK diverifikasi di device/browser sungguhan, jujur ditandai sebagai limitasi. Live-verified semua 6 item terhadap stack real, deploy build-dulu-swap, ngrok `200` sepanjang proses. 0 schema/business-logic change, 0 dependency baru. |
| v35 | 2026-08-14 | FIX KRITIS: financialType dipilih saat create transaksi tidak pernah bikin row Payment/Deposit/Disbursement | src/lib/financialTransactionActions.ts, src/app/api/transactions/route.ts, src/app/api/transactions/[id]/classify/route.ts, src/app/api/payments/[id]/correct/route.ts | Ditemukan live dari laporan Tami: payment yang baru diinput nggak muncul di halaman Payments. Dicek langsung ke DB (bukan nebak) — transaksinya `financial_type='PAYMENT'` tapi row `payment`-nya TIDAK ADA sama sekali. Akar masalah: `POST /api/transactions` terima `financialType` dari form New Transaction tapi cuma nulis label-nya doang ke kolom, row anak (Payment/Deposit/Disbursement) yang sebenarnya dipakai `/payments`/`position.ts`/alokasi cuma pernah dibuat oleh endpoint classify terpisah — kalau Financial Type dipilih LANGSUNG pas create (bukan dibiarkan Unclassified lalu classify belakangan), jadi rusak permanen: invisible di Payments, nggak bisa dialokasikan, DAN nggak bisa diperbaiki dari UI sama sekali (panel classify cuma muncul kalau financialType===UNCLASSIFIED). File `correct/route.ts` sendiri sebenarnya sudah punya komentar yang tepat dokumentasiin invariant ini dan udah workaround manual buat 1 call site-nya — gap-nya di `POST /api/transactions` yang nggak pernah dikasih perlakuan sama. Fix: 2 fungsi baru di `financialTransactionActions.ts` (`assertFinancialTypeDirection`, `createClassificationRecordTx`) diekstrak dari `classify/route.ts`, dipakai bareng oleh 3 call site (classify direfactor, correct direfactor ganti workaround manual, DAN POST /api/transactions — ini yang jadi fix sungguhan, sekarang validasi direction + bikin row anak dalam `$transaction` yang sama, atomic). `createFinancialTransactionTx` sengaja TIDAK disentuh biar nggak double-create di `correct` route. **Data Tami diperbaiki live** — bukan tempel SQL mentah, panggil endpoint classify yang sudah fix langsung ke transaksinya (audit trail proper, notes jelas ini backfill bug). Live-verified: reproduksi bug dulu (kosongkan row payment terbukti), fix, reproduksi ulang (row payment sekarang kebuat + muncul di /api/payments), transaksi test di-void (bukan dihapus). 160/160 test, 0 regresi, migrate deploy bersih dari nol. Deploy build-dulu-swap, ngrok `200` sepanjang proses. 0 schema change. |
| v42 | 2026-08-15 | Fix: "Deposit Remaining" negatif menyesatkan — ditemukan dari feedback UAT akuntan asli (Tami) | src/components/FinancialPositionView.tsx, src/lib/fieldHelp.ts | Tami tanya beruntun soal Disbursement (wajib atau tidak, harus sama dengan Rincian Biaya atau tidak, apakah otomatis kehitung pendapatan-dikurangi-pengeluaran) — ditelusuri ke KODE-nya (bukan cuma dijelaskan ulang), ketemu penyebab konkret: `position.ts`'s `depositUsed` itu literally = `disbursementTotal` (semua transaksi Disbursement matter itu, TIDAK terikat ke deposit yang benar-benar ada), tapi Advanced mode nampilin "Deposit Received/Used/Remaining" TANPA SYARAT — jadi matter yang punya disbursement tapi tidak pernah punya deposit nampilin "Deposit Remaining: -Rp X" yang menyesatkan (kesannya client punya utang deposit, padahal tidak). Tooltip `depositUsed` juga salah — bilang "dana yang dipakai untuk keperluan deposit itu" padahal kodenya tidak scoped begitu. Lite Mode sudah benar sejak v12 (sembunyikan baris deposit kalau depositReceived=0) — Advanced mode sengaja dibiarkan sejak itu, feedback Tami jadi bukti buat direvisit. Diperbaiki di 4 tempat, akar masalah sama: (1) KPI tiles Advanced mode — Received/Used/Remaining sekarang cuma muncul bareng kalau depositReceived>0 (angka disbursement sendiri tetap kelihatan di card Disbursement, tidak hilang, cuma tidak dobel dengan label menyesatkan); (2) tabel "Per Matter" — tampil "—" bukan angka negatif menyesatkan; (3) subtitle card "Deposit / Funds" — ganti teks kalau belum ada deposit; (4) `fieldHelp.ts`'s `depositUsed` dikoreksi sesuai kode asli. Ditambah subtitle baru di card Disbursement yang langsung jawab pertanyaannya: opsional, tidak wajib sama dengan Rincian Biaya, tidak ada perhitungan margin otomatis (sudah eksplisit dibahas+ditunda dengan Tami di v12). 0 schema/API/business-logic change — formula position.ts tidak disentuh, ini murni soal tidak menampilkan angka yang menyesatkan. 129/129 unit test tetap pass, tsc/lint/build bersih. Limitasi sama seperti sejak v13/v14: tidak ada browser tooling, tampilan asli belum diverifikasi visual langsung. |
| v41 | 2026-08-15 | Koreksi nama admin + break-glass PIN reset script | prisma/migrations/20260815090000_add_staff_admin_and_pin_reset/migration.sql, scripts/break-glass-reset-pin.sh (baru) | Follow-up v40 di hari yang sama. (1) Koreksi: migration v40 bootstrap isAdmin ke "Pani" — ternyata Pani staf biasa, admin yang benar adalah **Irfani Utami** (orang berbeda). Migration belum pernah diterapkan ke DB persisten manapun (cuma ke container test disposable yang sudah dihapus), jadi file migration dikoreksi langsung di tempat, bukan bikin migration susulan. (2) `scripts/break-glass-reset-pin.sh` baru — jawab pertanyaan user langsung "bagaimana kalau Irfani lupa PIN-nya sendiri?": dengan cuma 1 admin (sesuai keputusan "1 orang dulu"), tidak ada yang bisa pakai reset in-app (perlu login sebagai admin dulu) kalau akun itu sendiri yang terkunci. Script ini jalur terakhir: butuh akses server/Docker langsung (sengaja tidak reachable dari web app — alasan yang sama kenapa SMTP ditolak dari awal), generate PIN baru + hash pakai algoritma PERSIS sama dengan app (`scrypt`, `hashPin()` di session.ts) supaya dijamin bisa dipakai login sungguhan, bukan cuma "mirip" hash valid. **Bug nyata ketemu+diperbaiki saat verifikasi**: draft pertama pakai `psql -c "... :'var' ..."` untuk safe-interpolation — ternyata psql TIDAK melakukan substitusi `:'var'` ke teks SQL dalam mode `-c` (cuma jalan untuk meta-command `\echo`, bukan SQL biasa) — quirk psql nyata, bukan typo. Diperbaiki jadi pipe via stdin heredoc (pola sama dengan migrate.sh/restore-drill.sh yang sudah ada). Diverifikasi ulang: nama staf pakai apostrof (`O'Brien Test`) round-trip benar lewat match-count guard yang tadinya juga diam-diam rentan (bash string interpolation mentah). Hash generation dicek ke `verifyPin()` asli via tsx (bukan reimplementasi) — cocok byte-for-byte. Migration terkoreksi diterapkan ulang ke Postgres disposable dari nol, full suite 182/182 pass, tsc/lint bersih. **TIDAK diverifikasi**: `docker compose exec app/db` sungguhan ke stack Compose asli — stack project ini sedang live (melayani tunnel UAT yang sedang berjalan), bikin stack kedua dari docker-compose.yml yang sama akan collide di port host (3000, 5432) dan image tag `notary-accounting-app:latest`. Setiap bagian yang didepend script ini diverifikasi terpisah (algoritma hash ke verifyPin asli, SQL persis ke DB disposable) — dicatat jujur sebagai gap eksplisit, bukan klaim full end-to-end pass. |
| v40 | 2026-08-15 | Admin-restricted PIN reset (forgot-PIN painkiller, tanpa SMTP/RBAC penuh) | prisma/schema.prisma, prisma/migrations/20260815090000_add_staff_admin_and_pin_reset (baru), src/lib/staffActions.ts (baru), src/app/api/staff/[id]/reset-pin/route.ts (baru), src/components/StaffList.tsx (baru), src/app/(app)/settings/page.tsx, tests/helpers/callApi.ts, tests/scenarios/staffPinReset.test.ts (baru), ddl_notary_financial_control.sql | Follow-up dari analisis "SMTP untuk forgot-PIN?" — ditolak (Staff tidak punya kolom email, jadi dependency internet outbound pertama untuk flow auth-critical, login-nya staff-picker bukan email/username). Follow-up kedua "berarti butuh RBAC?" — juga ditolak eksplisit (full role engine bertentangan dengan CLAUDE.md §4 non-goal "complex approval workflow" dan komentar Staff model sendiri "not a permission/role system"). Solusi terkecil yang dipilih: `Staff.isAdmin` — SATU boolean menggerbangi SATU aksi, bukan sistem role, direstriksi eksplisit ke satu orang (office manager) sesuai keputusan user. Migration widen CHECK constraint audit_log (pola sama persis dengan migration bank_account) + bootstrap grant utk nama staf yang diminta (no-op di environment tanpa nama itu, mis. CI). `staffActions.ts` baru: generate PIN 6-digit via `node:crypto` (bukan Math.random), `assertIsAdmin` selalu re-check ke DB (bukan percaya session cookie), reset PIN juga clear login-lockout target (biasanya dua kejadian ini bareng). Route baru return PIN plaintext SEKALI di response buat admin sampaikan langsung — tidak pernah disimpan/di-log plaintext di manapun. UI: badge "Admin" + tombol "Reset PIN" (khusus viewer yang isAdmin, UX only bukan security boundary) di Settings > Staf Aktif, modal 2-langkah (confirm → one-time-reveal) pola sama dengan BackupRecoveryActions.tsx. `callApi.ts` (test helper) diperluas terima `staffId` override opsional (default ke ID lama, fully backward compatible) karena route ini butuh test admin-vs-non-admin dengan staffId DB berbeda, bukan cuma session presence. **Verifikasi live sungguhan** (bukan cuma typecheck): migration diterapkan ke Postgres disposable fresh, bootstrap grant dikonfirmasi cuma kena 1 nama yang diminta, CHECK constraint dikonfirmasi terima STAFF/PIN_RESET tapi tetap tolak value invalid asli (bukan dibuka lebar sembarangan). Scenario test baru 5/5 pass ke DB+route handler asli: non-admin→403, unauthenticated→401, target tidak ada→404, admin reset→PIN lama provably berhenti verify + PIN baru provably verify (`verifyPin()`), audit entry ter-atribusi ke nama admin yang bertindak. Full suite 182/182 pass (naik dari 129, +5 baru +48 existing scenario tidak regresi). tsc/lint/build bersih. Sengaja TIDAK dibangun: self-service "ganti PIN sendiri saat sudah login" — beda masalah (rotasi proaktif) dari "locked-out, tidak bisa self-serve" yang diselesaikan di sini. |
| v39 | 2026-08-14 | 3 DevOps quick-wins lanjutan: git-SHA image tag+rollback, pre-migration snapshot, capacity tracking | docker-compose.yml, scripts/deploy.sh (baru), scripts/rollback.sh (baru), scripts/migrate.sh (baru), scripts/capacity-report.sh (baru), docs/DEPLOYMENT.md | Menutup item 4-6 dari review arsitektur DevOps yang sama dengan v38. Juga menjawab pertanyaan langsung sesi ini: Grafana/Prometheus atau Elastic/Kibana over-engineering untuk stack ini (kantor tunggal, tanpa staf IT dedicated, bertentangan dengan keputusan "no alerting infrastructure" yang sudah ada di §13) — ditolak eksplisit, bukan diimplementasi; script di bawah adalah alternatif seukuran yang sesuai pola project ini. (4) Rollback sebelumnya = rebuild-and-pray karena `docker compose build` menimpa image lokal in-place, tidak ada build lama tersisa. Fix: `app` service dapat `image:` eksplisit (`notary-accounting-app:${APP_IMAGE_TAG:-latest}`), `deploy.sh` tag tiap build dengan git SHA + retag `:latest` + prune >3 build terlama (by build time, bukan urutan tag — SHA git tidak terurut kronologis), `rollback.sh <tag>` switch balik lewat `docker compose up -d app` asli tanpa rebuild (fallback ke `git checkout`+redeploy kalau tag sudah ke-prune); didokumentasikan eksplisit TIDAK rollback database. (5) Pre-migration safety net — `migrate.sh` ambil snapshot `pg_dump|gzip` langsung sebelum `migrate deploy` (folder terpisah dari `backup` service, retensi sendiri 10 snapshot), refuse kalau snapshot kosong. (6) Capacity tracking — `capacity-report.sh` snapshot row count+`pg_total_relation_size` 14 tabel (dari nama `@@map` di schema.prisma) + total DB size, di-append ke CSV, direkomendasikan cron mingguan. **Verifikasi live nyata** (bukan cuma baca kode): pipe `pg_dump|gzip` + `prisma migrate deploy` (6 migration) dijalankan ke container Postgres disposable yang di-seed schema asli; 14 query per-tabel `capacity-report.sh` + query total DB size dijalankan ke container yang sama, semua resolve benar; logic prune `deploy.sh` dites ke 5 dummy tagged image dengan waktu build berbeda (benar: keep 3 terbaru+latest, hapus 2 tertua); `docker compose config` konfirmasi `image:` baru resolve ke `notary-accounting-app:latest` by default. **Sengaja TIDAK diverifikasi**: siklus `docker compose up -d`/`rollback.sh` sungguhan ke `app` service project ini sendiri — sesi ini menemukan volume `db_data`/`attachments_data` yang sudah ter-provision di checkout dan tidak bisa memastikan itu deployment aktif atau bukan, jadi memilih tidak mengganggu daripada berisiko. 0 schema/API/business-logic change. |
| v38 | 2026-08-14 | 3 DevOps quick-wins: attachments-backup health signal, image digest pinning, CI pipeline | docker-compose.yml, Dockerfile, .github/workflows/ci.yml (baru), docs/DEPLOYMENT.md | Dari review arsitektur DevOps read-only (3 "quick win" dipilih dari daftar temuan). (1) `attachments-backup` sebelumnya nol sinyal kegagalan — loop `while true` tanpa error handling bisa gagal tiap malam (disk penuh, dll) tapi container tetap "running" selamanya, gap sekelas bug v31 tapi belum pernah kepegang untuk service ini. Fix: status file per iterasi + Docker healthcheck asli (butuh file <26 jam DAN isi "OK") — `check-health.sh` otomatis dapat manfaatnya tanpa perlu diubah. Diverifikasi langsung ke busybox asli (bukan asumsi GNU coreutils) untuk 3 state: fresh+OK, fresh+FAILED, stale-meski-OK. (2) Semua 5 image di-pin by digest (`node:20-alpine` di 4 stage Dockerfile, `postgres:16-alpine`, `caddy:2-alpine`, `prodrigestivill/postgres-backup-local:16-alpine`, `alpine:3`) — tag floating sebelumnya bisa diam-diam resolve ke build berbeda antar rebuild `--no-cache`. Digest ditarik+diverifikasi live sesi ini (bukan diketik manual), prosedur refresh terdokumentasi di DEPLOYMENT.md §13a. (3) CI pipeline baru (`.github/workflows/ci.yml`) — sebelumnya NOL, semua "verified" di histori project ini dikerjakan manual termasuk sesi ini sendiri. 2 job: `test` (lint→typecheck→full test suite termasuk tests/scenarios pakai Postgres service container asli→build) dan `docker-build` terpisah (buktikan Dockerfile sungguhan build bersih — layer yang sudah 2x bikin production rusak: v5 OpenSSL, v31 lockfile drift). Tidak sentuh model deployment (tetap manual Compose di server kantor). Semua diverifikasi live sesi ini: `docker compose config` resolve 5 digest, `docker build .` penuh sukses, healthcheck dites ke busybox asli, YAML CI di-parse (bukan cuma dibaca) via js-yaml. Belum diverifikasi jalan sungguhan di GitHub Actions runner (belum ada push). 0 schema/API/business-logic change. |
| v37 | 2026-08-14 | Settings > Backup & Recovery (read-only status UI) | src/lib/backupStatus.ts (baru), src/components/BackupRecoverySettings.tsx (baru), src/components/BackupRecoveryActions.tsx (baru), src/app/(app)/settings/page.tsx, docker-compose.yml, docs/DEPLOYMENT.md | Dibangun dari spec detail dengan aturan keamanan eksplisit (no fake API, no backup engine baru, no status "verified" palsu, no jargon infra di UI). 2 keputusan scope dikonfirmasi ke user sebelum implementasi, keduanya opsi paling aman: (1) "Create Backup Now" informational-only — app container tidak punya cara aman memicu service `backup` terpisah tanpa akses Docker socket (setara root ke host, ditolak) atau pg_dump sendiri (bikin jalur dump kedua yang bisa beda versi lagi — persis bug yang sudah diperbaiki di v31); (2) status Secondary/off-site backup jadi label statis jujur, bukan real-time — `offsite-sync.sh` (v36) jalan di host lewat cron, app container tidak punya visibilitas ke path disknya sama sekali. Yang REAL: `backupStatus.ts` baca `./backups/db`+`./backups/attachments` (mtime/size asli) lewat mount read-only baru di service `app` (`BACKUP_DB_DIR`/`BACKUP_ATTACHMENTS_DIR`), threshold freshness 48 jam reuse dari `check-health.sh` (v36, bukan angka baru), Backup History = listing asli bukan rekaan. Restore verification selalu tampil jujur "Belum pernah diverifikasi" (konsisten dengan limitasi v36 — belum pernah dites ke disk kedua sungguhan). Restore UX: checkbox konfirmasi risiko tinggi lalu tampilkan 11-langkah prosedur manual sebagai dokumentasi, BUKAN eksekusi — tidak ada endpoint restore dibuat sama sekali. Ditempatkan di Settings yang sudah ada (BUKAN item sidebar baru) sesuai instruksi eksplisit; tidak ada split admin/accountant karena dicek langsung ke session.ts — app ini memang tidak punya role/permission model sama sekali (flat staff+PIN), jadi gating role akan jadi rekayasa baru, bukan reuse. 0 schema change, 0 endpoint API baru (status dibaca server-side langsung oleh Settings page, pola sama dengan `getBrandingSettings()`), 0 business logic change. 17 test baru pakai temp directory asli (bukan mock fs) — 129/129 unit test pass. tsc/lint/build (41 route) diverifikasi bersih lewat container `node:20-alpine` sekali pakai (Node/npm tidak ada di PATH host sandbox ini) — `docker compose config` juga konfirmasi 2 mount read-only baru resolve dengan benar. Belum diverifikasi live ke deployment yang benar-benar jalan di sesi ini. |
| v36 | 2026-08-14 | Automated off-machine backup sync ke second internal disk | scripts/offsite-sync.sh (baru), scripts/check-health.sh, docs/DEPLOYMENT.md | Menutup gap DR yang didokumentasikan di v15/v31: `./backups/db`+`./backups/attachments` satu disk fisik dengan live DB, mitigasi sebelumnya cuma manual "copy USB mingguan" tanpa jejak. Dikonfirmasi ke user: device kedua = second internal disk always-mounted (bukan external HDD periodik) — desain jadi cron host-level + `rsync`, bukan container baru (file backup sudah plain file di host lewat bind mount yang ada). 2 keputusan desain utama: (1) TIDAK pakai `rsync --delete` — mirror cuma nambah/update, di-prune terpisah pakai retensi sendiri 90 hari (lebih panjang dari retensi primary 14 hari) supaya event buruk di primary (ransomware/`rm` salah) tidak ikut menghapus satu-satunya salinan penyelamat; (2) mount-check guard (`mountpoint -q`) sebelum sync — tanpa ini, disk kedua yang gagal mount setelah reboot akan bikin script "berhasil" nulis ke folder kosong di disk root yang sama, membatalkan seluruh tujuannya. `check-health.sh` ditambah staleness check yang **opt-in** (WARN kalau `OFFSITE_BACKUP_DIR` di-set tapi tidak ada file ke-sync 48 jam terakhir) — deployment yang belum pakai fitur ini tidak berubah outputnya sama sekali. `docs/DEPLOYMENT.md` §14 baru (runbook lengkap: fstab UUID, install rsync, crontab, verifikasi), §5 di-reframe menunjuk ke situ, guidance manual off-site tetap dipertahankan (ini cuma nutup disk failure, bukan kehilangan seluruh server/chassis — fire/theft/flood tetap butuh copy manual yang benar-benar keluar gedung). 0 schema/API/docker-compose change, 0 dependency baru selain `rsync` (host-level, bukan masuk image Docker manapun). **Belum diverifikasi live** — tidak ada disk kedua sungguhan di environment ini untuk dites, sama seperti limitasi v29 sebelum cutover live v30; jadi langkah lanjutan begitu disk fisiknya terpasang di kantor. |

<!-- rtk-instructions v2 -->
# RTK (Rust Token Killer) - Token-Optimized Commands

## Golden Rule

**Always prefix commands with `rtk`**. If RTK has a dedicated filter, it uses it. If not, it passes through unchanged. This means RTK is always safe to use.

**Important**: Even in command chains with `&&`, use `rtk`:
```bash
# ❌ Wrong
git add . && git commit -m "msg" && git push

# ✅ Correct
rtk git add . && rtk git commit -m "msg" && rtk git push
```

## RTK Commands by Workflow

### Build & Compile (80-90% savings)
```bash
rtk cargo build         # Cargo build output
rtk cargo check         # Cargo check output
rtk cargo clippy        # Clippy warnings grouped by file (80%)
rtk tsc                 # TypeScript errors grouped by file/code (83%)
rtk lint                # ESLint/Biome violations grouped (84%)
rtk prettier --check    # Files needing format only (70%)
rtk next build          # Next.js build with route metrics (87%)
```

### Test (60-99% savings)
```bash
rtk cargo test          # Cargo test failures only (90%)
rtk go test             # Go test failures only (90%)
rtk jest                # Jest failures only (99.5%)
rtk vitest              # Vitest failures only (99.5%)
rtk playwright test     # Playwright failures only (94%)
rtk pytest              # Python test failures only (90%)
rtk rake test           # Ruby test failures only (90%)
rtk rspec               # RSpec test failures only (60%)
rtk test <cmd>          # Generic test wrapper - failures only
```

### Git (59-80% savings)
```bash
rtk git status          # Compact status
rtk git log             # Compact log (works with all git flags)
rtk git diff            # Compact diff (80%)
rtk git show            # Compact show (80%)
rtk git add             # Ultra-compact confirmations (59%)
rtk git commit          # Ultra-compact confirmations (59%)
rtk git push            # Ultra-compact confirmations
rtk git pull            # Ultra-compact confirmations
rtk git branch          # Compact branch list
rtk git fetch           # Compact fetch
rtk git stash           # Compact stash
rtk git worktree        # Compact worktree
```

Note: Git passthrough works for ALL subcommands, even those not explicitly listed.

### GitHub (26-87% savings)
```bash
rtk gh pr view <num>    # Compact PR view (87%)
rtk gh pr checks        # Compact PR checks (79%)
rtk gh run list         # Compact workflow runs (82%)
rtk gh issue list       # Compact issue list (80%)
rtk gh api              # Compact API responses (26%)
```

### JavaScript/TypeScript Tooling (70-90% savings)
```bash
rtk pnpm list           # Compact dependency tree (70%)
rtk pnpm outdated       # Compact outdated packages (80%)
rtk pnpm install        # Compact install output (90%)
rtk npm run <script>    # Compact npm script output
rtk npx <cmd>           # Compact npx command output
rtk prisma              # Prisma without ASCII art (88%)
rtk uv run <cmd>        # Compact uv project command output
```

### Files & Search (60-75% savings)
```bash
rtk ls <path>           # Tree format, compact (65%)
rtk read <file>         # Code reading with filtering (60%)
rtk grep <pattern>      # Search grouped by file (75%). Format flags (-c, -l, -L, -o, -Z) run raw.
rtk find <pattern>      # Find grouped by directory (70%)
```

### Analysis & Debug (70-90% savings)
```bash
rtk err <cmd>           # Filter errors only from any command
rtk log <file>          # Deduplicated logs with counts
rtk json <file>         # JSON structure without values
rtk deps                # Dependency overview
rtk env                 # Environment variables compact
rtk summary <cmd>       # Smart summary of command output
rtk diff                # Ultra-compact diffs
```

### Infrastructure (85% savings)
```bash
rtk docker ps           # Compact container list
rtk docker images       # Compact image list
rtk docker logs <c>     # Deduplicated logs
rtk kubectl get         # Compact resource list
rtk kubectl logs        # Deduplicated pod logs
```

### Network (65-70% savings)
```bash
rtk curl <url>          # Compact HTTP responses (70%)
rtk wget <url>          # Compact download output (65%)
```

### Meta Commands
```bash
rtk gain                # View token savings statistics
rtk gain --history      # View command history with savings
rtk discover            # Analyze Claude Code sessions for missed RTK usage
rtk proxy <cmd>         # Run command without filtering (for debugging)
rtk init                # Add RTK instructions to CLAUDE.md
rtk init --global       # Add RTK to ~/.claude/CLAUDE.md
```

## Token Savings Overview

| Category | Commands | Typical Savings |
|----------|----------|-----------------|
| Tests | vitest, playwright, cargo test | 90-99% |
| Build | next, tsc, lint, prettier | 70-87% |
| Git | status, log, diff, add, commit | 59-80% |
| GitHub | gh pr, gh run, gh issue | 26-87% |
| Package Managers | pnpm, npm, npx | 70-90% |
| Files | ls, read, grep, find | 60-75% |
| Infrastructure | docker, kubectl | 85% |
| Network | curl, wget | 65-70% |

Overall average: **60-90% token reduction** on common development operations.
<!-- /rtk-instructions -->