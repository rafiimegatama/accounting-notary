// Honest placeholder — Reports/Export is explicitly NICE-TO-HAVE (Step 21),
// not built in this MVP. This page exists so the nav link doesn't 404,
// and says so plainly instead of pretending the feature exists.
export default function ReportsPage() {
  return (
    <div>
      <h1>Reports / Export</h1>
      <p style={{ opacity: 0.7 }}>
        Belum tersedia di MVP ini. Semua data yang mendasari laporan sudah bisa dilihat lewat Client/Matter Financial Position
        dan Search — export/print formal ditandai sebagai NICE-TO-HAVE (lihat MVP_SCOPE.md).
      </p>
    </div>
  );
}
